技术难点：
1、校车功能：
	预期效果：
		进入校车页面，开始读取该幼儿园所有在用校车的信息，一次性读取来自各个校车上安装的gps定位器发送的数据，响应到页面上
	实现方式：
		1、socket编程：服务器端口监听，实时接收端口接收到的数据
			缺点：用到线程，访问校车页面的请求过多时，服务器会崩溃；
		2、mq消息队列：
			缺点：一条一条的接收，不能保证所有的校车定位数据都接收到并响应到页面上
		3、rpc远程调用：需要的时候就去各个校车上读取定位数据
			缺点：需要自己写一个定位程序，并实现远程调用
			此方法暂时可行